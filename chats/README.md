# Design transcripts

Conversation records from the Claude Design sessions that produced the
prototypes in `project/`, kept as provenance for why the design is the way it
is.

`chat1.md` has been removed, and the opening of `chat2.md` redacted: both
covered a different client's engagement, which this project only inherited a
layout system from. Nothing in them related to MBC Consultancy.

The remaining transcripts run in order: `chat2.md` (design system established
for MBC), `chat3.md` (rebuild against the client brief, palette and motion),
`chat4.md` (final state).
